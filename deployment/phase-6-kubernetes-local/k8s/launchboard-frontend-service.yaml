# Frontend Service exposes the web UI.
apiVersion: v1
kind: Service
metadata: {name: launchboard-frontend, namespace: devops-launchboard}
spec: {selector: {app: launchboard-frontend}, ports: [{name: http, port: 80, targetPort: 8080}]}
