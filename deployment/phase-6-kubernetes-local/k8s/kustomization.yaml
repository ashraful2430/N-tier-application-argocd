apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [namespace.yaml, configmap.yaml, secret.yaml, pvc.yaml, launchboard-backend-deployment.yaml, launchboard-frontend-deployment.yaml, launchboard-backend-service.yaml, launchboard-frontend-service.yaml, ingress.yaml, hpa.yaml]
