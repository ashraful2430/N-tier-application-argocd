resource "aws_security_group" "cluster" { name = "${var.project_name}-eks-cluster" description = "EKS control plane security group" vpc_id = var.vpc_id egress { from_port = 0 to_port = 0 protocol = "-1" cidr_blocks = ["0.0.0.0/0"] } tags = var.tags }
