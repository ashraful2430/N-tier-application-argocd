apiVersion: v1
kind: Service
metadata: {name: launchboard-backend-canary, namespace: devops-launchboard}
spec: {selector: {app: launchboard-backend}, ports: [{name: http, port: 8000, targetPort: 8000}]}
